name: Schedule Test

on:
  schedule:
    - cron: "17,47 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "EVENT=${{ github.event_name }}"
          date -u
